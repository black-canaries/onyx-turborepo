"use client";

/**
 * Client-only exports for @repo/ui-native
 *
 * This file exports components that must run on the client side only.
 * Currently empty, but follows the pattern from @repo/ui for consistency.
 */

// Future: Export client-only components here (e.g., form components with hooks)
